# 1、编程技术

- ## Windows下如何查看某个端口被谁占用

  - 查找所有运行的端口

    ```cmd
    netstat -ano
    ```

  - 查看被占用端口对应的 PID

    ```cmd
    netstat -aon|findstr "8080"
    ```

  - 查看指定 PID 的进程

    ```cmd
    tasklist|findstr "10508"
    ```

  - 结束进程

    ```cmd
    taskkill /T /F /PID 10508 
    ```

- 